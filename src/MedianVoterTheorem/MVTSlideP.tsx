import { Typography } from "@mui/material";
import { StyledSlideContainer } from "../Slides/Slides.styles";

export const MVTSlideP = () => {
    return (
        <StyledSlideContainer>
            <Typography>We've completed everything in this chapter!</Typography>
            <Typography>
                Next, we're going to look at another application in the field of
                soccor, paticularly in the penalty kick game. We'll explore
                methods to help us forming the best response.
            </Typography>
        </StyledSlideContainer>
    );
};
